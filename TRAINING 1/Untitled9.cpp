#include<bits/std>
using
#define ft(1,x,y) for(int i = x;i < y;i++)
int a[1000000];
int n;
int *Max,*Re_Max;
void input(){
	cin>>n;
	ft()
	cin>>a[i];
	Max = new int(n+2);
	Re_Max = new int(n+2);
}
int *Max,*Re_Max;
void process(){
	Max[0] = -1;
	ReMax[n+1] = -1;
	ftd()
	Re_Max[i] = max(Re_Max[i+1],a[i]);
}

int main(){
	return 0;
}
