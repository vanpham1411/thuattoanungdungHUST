#include<bits/stdc++.h>

using namespace std;
typedef pair<int, int> pairs;
int main(){
	pair<int,int> p;
	p = make_pair(2,3);
	p.first = p.first+1;
	cout<<p.first;
	set<pairs> s;
	s.insert(make_pair(3, 4));
	s.insert(make_pair(0,0));
	if()
}
